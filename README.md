# weather-forecast-app

first go to the project and run this command to install the node modules-npm install
to run the project run this command=npm run start

to install all dependencies run -npm i
in .env folder replace the REACT_APP_API_ID with you own api id from open weather
to run the application - npm run start
